<g:hiddenField name="variants[${variantIndex}].attributez[${attributeIndex}].id" value="${attribute?.id ?: ''}" />
<g:hiddenField name="variants[${variantIndex}].attributez[${attributeIndex}].sku" value="${attribute?.sku}" />
<g:hiddenField name="variants[${variantIndex}].attributez[${attributeIndex}].storeId" value="${attribute?.storeId}" />
<g:hiddenField name="variants[${variantIndex}].attributez[${attributeIndex}].value" value="${attribute?.value}" />
<g:hiddenField name="variants[${variantIndex}].attributez[${attributeIndex}].productAttributeId" value="${attribute?.productAttributeId}" />
<g:hiddenField name="variants[${variantIndex}].attributez[${attributeIndex}].attributeName" value="${attribute?.attributeName}" />
<g:hiddenField name="variants[${variantIndex}].attributez[${attributeIndex}].attributeType" value="${attribute?.attributeType}" />
